meta {
  name: Mongo Retry User Add
  type: http
  seq: 10
}

post {
  url: http://localhost:8080/mongo-retry/users
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Username Test",
    "email": "email@email.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
